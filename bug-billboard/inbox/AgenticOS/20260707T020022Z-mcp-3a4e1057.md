---
id: 20260707T020022Z-mcp-3a4e1057
discovered: 2026-07-07T02:00:22Z
agent: mcp:os-tools
source: mcp
severity: low
status: open
route: /mobile/ task-inbox section
project: AgenticOS
---
# Task inbox bridge (/shared-billboard/tasks/INDEX.md) is a permanent 404 — section is always "source offline" + console error on every load/refresh tick

## Description
Matrix scrutiny of /mobile/ (2026-07-06, all 6 devices): m.js L478 fetches /shared-billboard/tasks/INDEX.md which 404s (verified curl via the 43702 server); ~/.claude/shared-billboard has no tasks/ dir, so the bridge target never existed. The Task inbox section therefore shows the dim "◌ task inbox (no store / bridge yet) — source offline" chip on every device, and each 60s refresh tick logs a fresh 404 console error (1 per load observed in the desktop Playwright pass). The chip IS the designed honest-degradation state, so this is low — but it is a permanently-dead section shipped on the page. Fix candidates (user decides): (a) create the tasks store + bridge symlink so the section goes live; (b) drop/hide the section until the store exists; (c) suppress the repeat 404 by probing once per session. Evidence: reports/captures/playwright/pw-393x851-android-small-full.png (offline chip) + Playwright console log (single 404 per navigation).

_Logged via the os-tools MCP `bug_log` (append-only inbox — main consolidates; this writer never touches master.md)._
