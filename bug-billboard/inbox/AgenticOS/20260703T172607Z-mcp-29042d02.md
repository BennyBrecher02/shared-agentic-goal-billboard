---
id: 20260703T172607Z-mcp-29042d02
discovered: 2026-07-03T17:26:07Z
agent: mcp:os-tools
source: mcp
severity: medium
status: open
route: portable-kit/deploy-kit.sh (init verb)
project: AgenticOS
---
# deploy-kit init stamps a fixed dev port (43700) — collides with the OS repo + every other tenant

## Description
Found by the FIRST real sandbox run of `deploy-kit.sh init` (woodshed, 2026-07-03). The init verb stamps project-config.json with the same defaults for every project: dev_server=43700, preview=43702, e2e=43703 — the exact ports the OS repo itself owns (astro_dev/dashboard/e2e_playwright). Two tenants can never dev-serve at once, and the port shows up in THREE stamped files (project-config.json + AGENTS.md prose + playwright.config.ts), which had to be hand-sed'd to 43730/43732/43733 for woodshed. FIX: init should allocate a per-tenant port block (e.g. next free 437x0 decade probed via lsof/registry of stamped tenants — it already registers tenants on the shared billboard, so the registry knows what's taken) and stamp ALL THREE files from that one value. Severity med: silent-collision class, hits every future init.

_Logged via the os-tools MCP `bug_log` (append-only inbox — main consolidates; this writer never touches master.md)._
