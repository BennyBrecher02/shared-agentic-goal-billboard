---
id: 20260707T020012Z-mcp-3a4de7f7
discovered: 2026-07-07T02:00:12Z
agent: mcp:os-tools
source: mcp
severity: low
status: open
route: /mobile/ goals section + goal-billboard G21 data
project: AgenticOS
---
# G21 goal card renders with no priority badge — upstream malformed priority ('|-' YAML block-scalar artifact) silently dropped by the badge regex

## Description
Matrix scrutiny of /mobile/ (2026-07-06, all 6 devices): the Active Goals list shows a P-badge on every card except G21, whose /billboard/data.json priority is the literal string "|-" (a malformed YAML block-scalar left in the goal file). m.js L397 `/^\s*(P\d)/.exec(g.priority)` finds no match and renders NO badge — visually inconsistent with the other 10 cards (blank gap where the badge sits) and the goal silently sorts last regardless of true priority (prioRank on unparsed string). Related same-class item: bug #B1 renders as "untitled — details missing in the billboard" because master.md's lone entry has `?` placeholder fields — the page is honest but the upstream entry is malformed. Fix candidates (user decides): (a) repair the G21 goal file's priority field (+ the #B1 entry) upstream; (b) optionally have m.js render an explicit "P?" badge when priority exists but doesn't parse, so malformed data is visible rather than blank. Evidence: reports/captures/playwright/pw-834x1194-ios-tablet-full.png (G21 row, badge-less) + reports/captures/ios/iPad-Pro-11-M5-validated-20260706-215707.png.

_Logged via the os-tools MCP `bug_log` (append-only inbox — main consolidates; this writer never touches master.md)._
